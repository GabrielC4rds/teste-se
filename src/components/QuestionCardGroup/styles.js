import styled, { css } from "styled-components";

export const Content = styled.div`
    width: 55%;
    display: flex;
    flex-direction: column;
    align-items: start;
    div{
      flex-wrap: wrap;
      display: flex;
    }
`;